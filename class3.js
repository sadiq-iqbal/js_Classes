// logical operator in JS
// assigmnet operator "=" this assign the value to the variable
// equality operator :->
    // == double equal just check for the value and not type
    // === it looks for the value + type and returns a boolean

// less than operator " < "
    // it checks whether one value is lesser or not 

// greater than operator " > "
    // It checks whether one of the two is greater than the other or not if yes it returns true

// greater than or equal to  >=
    // it checks for two situation if any or both gets true it returns true
        // a >= b 

// lesser than or equal to 
    // it checks for two situation if any or both gets true it returns true
        // a <= b 


// logical and operator &&
// logical or operator ||
// not equals to !


